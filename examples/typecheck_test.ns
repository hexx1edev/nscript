fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() -> i32 {
    let x: i32 = 5;
    let y: u8 = 3;

    if x > 10 {
        x = x - 1;
    } else if y == 3 {
        x += add(x, 2);
    } else {
        x = 0;
    }

    return x;
}
